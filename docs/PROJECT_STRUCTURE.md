# 项目结构说明

- `frontend/` 只负责浏览器中的页面、中文/英文切换、图谱画布与交互。
- `backend/` 提供 REST API、图数据加载、节点与关系搜索、推理和治理能力。
- `knowledge-graph/` 保存港中深数据科学学院专用的数据、本体、采集与构图逻辑。
- `scripts/` 提供开发者常用的一键启动入口。

这种拆分让网页、后端框架和学院数据可以分别维护；更换网页样式不会影响数据，
更新官网数据也不需要改动通用后端代码。

